<%
	var title = page.title || ''
	var keywords = page.keywords || ''
	if (is_archive()) {
		var title = '归档'
		} else if (is_category()) {
			title = page.category;
		} else if (is_tag()) {
			title = page.tag;
		} else if (is_home()) {
			title = '';
		}
%>

<title>
	<% if(title){ %>
		<%=theme.Website.name +" | " + title %>
		<% }else{ %>
		<%=theme.Website.name %>
		<% } %>
</title>
<meta http-equiv="content-type" content="text/html; charset=utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
<meta name="renderer" content="webkit">
<meta name="force-rendering" content="webkit">
<meta name="description" content="梁咩阿的生活记录，life is a fucking movie！">
<meta name="keywords" content="Lorre的梦呓" >
<meta name="referrer" content="no-referrer">
<link rel="shortcut icon" href='<%=theme.Website.logo%>'/>

<%- css('css/style.css') %>
<%- js('js/utils.js') %>
<%- js('js/main.js') %>